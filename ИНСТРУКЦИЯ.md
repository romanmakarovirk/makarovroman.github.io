# Инструкция по запуску проекта

## 🚀 Как запустить проект

### Важно!
Это React приложение, его **нельзя** открывать напрямую через двойной клик на `index.html`. Нужно запустить dev сервер.

### Шаг 1: Откройте терминал

Откройте терминал в папке проекта:
```bash
cd /Users/romanmakarov/Downloads/portfolio
```

### Шаг 2: Запустите dev сервер

```bash
npm run dev
```

### Шаг 3: Откройте в браузере

После запуска вы увидите что-то вроде:
```
  VITE v5.4.21  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

Откройте в браузере адрес: **http://localhost:5173/**

## ⚠️ Если не открывается

### Проблема 1: Порт занят
Если порт 5173 занят, Vite автоматически предложит другой порт (например, 5174).

### Проблема 2: Ошибки в консоли
Откройте консоль браузера (F12) и проверьте ошибки.

### Проблема 3: Зависимости не установлены
Если видите ошибки про отсутствующие модули:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема 4: TypeScript ошибки
Проверьте ошибки:
```bash
npm run build
```

## 📝 Альтернативные команды

### Сборка для продакшена
```bash
npm run build
```

### Просмотр собранной версии
```bash
npm run build
npm run preview
```

## 🔍 Что должно отображаться

После запуска вы должны увидеть:
- Анимированный WebGL фон (цветные волны)
- Текст "Design is Everything"
- Кнопку "Let's Go" с эффектом жидкого стекла

## 💡 Полезные команды

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка проекта
- `npm run preview` - просмотр собранной версии
- `Ctrl+C` - остановка dev сервера
